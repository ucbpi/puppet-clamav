# since we use some git repos in bundler in addition to normal gems, we need
# to run bundler setup for it to work
require 'bundler'
Bundler.setup

# include our puppetlabs spec helper rake tasks
require 'puppetlabs_spec_helper/rake_tasks'
